<%- include header %>
	<p>
		<span><a href="/edit/<%= encodeURI(post.name) %>/<%= post.time.day %>/<%= encodeURI(post.title) %>" class="edit">编辑</a></span>
		<span><a href="/remove/<%= encodeURI(post.name) %>/<%= post.time.day %>/<%= encodeURI(post.title) %>" class="edit">删除</a></span>
	</p>
	<p class="info">
		作者：<a href="/u/<%= encodeURI(post.name) %>"><%= post.name %></a>
		日期：<%= post.time.minute %>
	</p>
	<p><%- post.post %></p>
<%- include footer %>